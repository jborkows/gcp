{
    "steps": [
     {
        "name": "gcr.io/${PROJECT_ID}/firebase",
        "args": [
           "deploy",
           "--project",
           "${PROJECT_ID}",
           "--only",
           "hosting"
         ],
         "dir": "firebase"
    }
    ],
    "options":{
        "logging": "CLOUD_LOGGING_ONLY"
    }
    
  }